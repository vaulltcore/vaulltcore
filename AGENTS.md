# Vaulltcore repo memory

- Monorepo: npm workspaces; packages export `src/index.ts` directly (no build step; vitest
  resolves TS). `npm test` = vitest, `npm run typecheck` = `tsc --build packages/*`.
- Phase 1A delivered a durable job runner; full report in `docs/phase1a.md`.
- Hard seam: `packages/vaulltcore-runner` (neutral, zero runtime deps) must never import
  from `packages/vaulltcore-runner-opencode` (adapter). Engine-specific types must not
  leak into the neutral contracts.
- Durability rules that must not regress: tool calls recorded before execution; commit
  boundary = `checkpoint` event + checksummed checkpoint file; resume validates checksum,
  identity, policy/engine versions, watermark before continuing; recorded-but-unsettled
  tool calls are never blindly re-executed.
- Deterministic engines (`ScriptEngine`, `ScriptModelProvider`) derive their step index
  from committed history (assistant message count), not instance state — fresh instances
  must resume correctly.
- Extracted OpenCode code is MIT; keep attribution headers and `NOTICE.md` current.

Phase 1C COMPLETE: 76/76 tests, report docs/phase1c.md.
- New packages: `packages/vaulltcore-store-sql` (transactional DurableJobStore over node:sqlite via SqlDatabase/SqlDialect seam -- Postgres-ready), `packages/vaulltcore-environment-cloud` (vendor-neutral CloudExecutionProvider + CloudExecutionEnvironment + FakeCloudProvider), `packages/vaulltcore-control` (thin HTTP facade over AgentRunner: POST/GET /jobs, SSE events, cancel, input, usage; replaceable auth boundary; Idempotency-Key registry).
- Runner contract additions (backward compatible): `getJob`, `listEvents`; `JobView` read model; `snapshot-policy.ts` (SnapshotPolicy/ThresholdSnapshotPolicy; advisory only, emits sanitized "snapshot_decision" warning events).
- Invariants that must not regress: SQL store keeps fenced CAS on every state-changing write; (job_id, seq) PRIMARY KEY rejects duplicate delivery; orphan events beyond checkpoint watermark never replay as committed history; cloud snapshot is optimization only -- checkpoint + event log stay authoritative; capability honesty: unsupported native ops throw CapabilityUnsupportedError, never pretend; control plane derives tenant from the authenticated principal, never from request body; snapshot policy can never suppress checkpoint durability.
- node:sqlite is experimental in Node 22: it is loaded via createRequire in store-sql/src/driver.ts so bundlers (vite/vitest) do not try to resolve the stripped "sqlite" id. Do not replace with a static import.
- Test loop gotcha: the runner loop only continues past a turn when the turn emitted tool calls; a text-only turn is terminal. Tests that need multi-turn jobs must give turn 0 a tool call.

Phase 1B COMPLETE (AX extraction): contracts extended in packages/vaulltcore-runner/src/contracts.ts (SuspensionReason, WorkspaceState, ExecutionSnapshot, ExecutionEnvironment, OwnershipGrant, ActorHandle, RecoveryContext, ExecutionActorController, JobRecord.latestSnapshot). environment.ts (LocalExecutionEnvironment: sha256(jobId) binding, manifest+per-file hash snapshots, same-host reattach). actor.ts (ExecutionActorControllerImpl: acquire/start/suspend/recover/resume/snapshot/release/destroy; recovery algorithm: validate→fence→checkpoint→events→workspace→snapshot→proceed; invalid continuation parks suspended). Store fenced appends (expectedAttempt) + releaseLease. Runner routes through controller. Tests 34/34; report docs/phase1b-actors.md.

Phase 1C COMPLETE: 76/76 tests, report docs/phase1c.md.
- New packages: `packages/vaulltcore-store-sql` (transactional DurableJobStore over node:sqlite via SqlDatabase/SqlDialect seam — Postgres-ready), `packages/vaulltcore-environment-cloud` (vendor-neutral CloudExecutionProvider + CloudExecutionEnvironment + FakeCloudProvider), `packages/vaulltcore-control` (thin HTTP facade over AgentRunner: POST/GET /jobs, SSE events, cancel, input, usage; replaceable auth boundary; Idempotency-Key registry).
- Runner contract additions (backward compatible): `getJob`, `listEvents`; `JobView` read model; `snapshot-policy.ts` (SnapshotPolicy/ThresholdSnapshotPolicy; advisory only, emits sanitized "snapshot_decision" warning events).
- Invariants that must not regress: SQL store keeps fenced CAS on every state-changing write; (job_id, seq) PRIMARY KEY rejects duplicate delivery; orphan events beyond checkpoint watermark never replay as committed history; cloud snapshot is optimization only — checkpoint + event log stay authoritative; capability honesty: unsupported native ops throw CapabilityUnsupportedError, never pretend; control plane derives tenant from the authenticated principal, never from request body; snapshot policy can never suppress checkpoint durability.
- node:sqlite is experimental in Node 22: it is loaded via createRequire in store-sql/src/driver.ts so bundlers (vite/vitest) do not try to resolve the stripped "sqlite" id. Do not replace with a static import.
- Test loop gotcha: the runner loop only continues past a turn when the turn emitted tool calls; a text-only turn is terminal. Tests that need multi-turn jobs must give turn 0 a tool call.

Phase 1D COMPLETE: 116/116 tests, report docs/phase1d.md.
- New packages: `packages/vaulltcore-worker` (WorkerHost, WorkerIdentity, fenced WorkerLease + heartbeat loop, JobReconciler), `packages/vaulltcore-environment-docker` (real DockerCloudProvider over the Docker Engine).
- New runner contracts (backward compatible): `packages/vaulltcore-runner/src/dispatcher.ts` (neutral JobDispatcher: enqueue/claim/acknowledge/heartbeat/release — the dispatcher decides where work goes, the runner decides how it executes), `packages/vaulltcore-runner/src/distributed.ts` (recovery eligibility; `RecoveryEligibilityReason` gains "orphaned").
- New store-sql: `SqlIdempotencyRegistry` + `SqlSnapshotRegistry` (`registries.ts`), `SqlDispatcher` + `PostgresDispatcher` (`dispatcher.ts`/`pg-dispatcher.ts`), `DistributedSqlStore` (`distributed-store.ts`), `PostgresJobStore` + `pg-migrations.ts` over the `pg` driver with SERIALIZABLE + FOR UPDATE fencing and int8-as-number parsing.
- Phase 1D rule proven: the HTTP process is never the long-running job owner; a request transactionally persists job + idempotency record then dispatches. Control plane and workers restart independently without losing job correctness.
- Invariants that must not regress: lease renewal is fenced (generation N-1 can never write once generation N is committed, across separate connections / a partition); recovery never blindly reruns uncertain non-idempotent tool calls; a superseded snapshot is collected ONLY when its replacement is `active` (not merely `created`) — never delete the last valid recovery artifact before its replacement is durably committed; DockerCloudProvider reports `durableWorkspace=false` honestly and throws CapabilityUnsupportedError for a missing image (never emulates); the worker requires no live HTTP server.
- Gated tests: postgres-conformance.test.ts and docker-provider.test.ts SKIP (not fail) when PostgreSQL/Docker is unavailable, so CI without those services stays green. Defaults: PG on unix socket /tmp/pgsock:5434 db vaulltcore_test; Docker via `DOCKER_CMD` (e.g. `sudo -n docker`). The root typecheck now builds `packages/*` (all packages, not just runner+opencode).

Phase 1E COMPLETE: 98/98 tests, report docs/phase1e.md.
- New packages: vaulltcore-identity (Tenant/Org/Member/Project/ProjectGrant/APIKey; API-key verifier hash + `.` separator, never plaintext; org/project-scoped authorize), vaulltcore-policy (versioned AdmissionPolicy -> immutable AdmissionDecision -> runner ExecutionPolicy; partial UNIQUE active index), vaulltcore-quota (race-free reservation state machine active/settled/released/expired/rejected; conditional in_use increment + UNIQUE request_key; fenced version on every transition), vaulltcore-metering (append-only idempotent UsageEvent; UNIQUE (tenant,job,kind,dedup_key); eventsToUsage adapter derives dedup keys from committed event seq), vaulltcore-billing (versioned PricingVersion; immutable LedgerEntry; UNIQUE (tenant,org,project,idempotency_key); no external payment processor), vaulltcore-audit (append-only; sanitizeMetadata redacts secret keys + opaque secret values; no update/delete path).
- Control-plane admission pipeline in vaulltcore-control/src/admission.ts (AdmissionPipeline): authenticate -> authorize -> idempotency -> policy.evaluate -> quota.reserve -> runner.createJob with compensation (release on creation failure). Extends, does not replace, existing facade.
- Invariants that must not regress: business layer never embedded in runner loop; tenant derived from authenticated principal never request body; least-privilege projectScope -- a principal with NO project grants has access to NO projects (the wildcard is never synthesized from absence); policyVersion pinned into JobRecord/JobCheckpoint and immutable after creation; exactly-once at the durable event/ledger identity boundary via UNIQUE + idempotency (execution stays at-least-once); reservation settlement/release fenced by version and idempotent; failed admission releases reservation (no leak); no plaintext secrets in audit/metering/billing/events.
- Reuses vaulltcore-store-sql SqlStoreBase + SqlDialect seam; all business stores apply their own migrations in-constructors. PostgreSQL concurrency tests are environment-gated SKIPs (PG not available in sandbox), not false passes.

Phase 1F COMPLETE: 158 passed / 17 skipped (gated), report docs/phase1f.md.
- New package: `packages/vaulltcore-reconcile` (ReconciliationService + SqlReconciliationStore; durable watermark as SOLE progress source; detects gaps A-H; repairs only safe missing downstream projections — rebuilds UsageEvents from committed JobEvents, retries pricing/ledger via durable idempotency, releases orphaned/terminal-unsettled reservations; NEVER re-executes agent steps or tool calls).
- New migrations: store-sql v8 `admission_idempotency` (durable distributed idempotency registry), v9 `reconciliation` (runs + gaps), v10 `snapshot_gc` (gc_attempts); billing v11 `usage_settlement` (settlement tracking); identity v12 `api_key_lifecycle` (expiry/rotation/last-used).
- Durable distributed idempotency: `SqlAdmissionIdempotencyRegistry` replaces in-memory registry; state machine pending→{completed|failed_retriable|failed_terminal}; claim() is the linearization point (UNIQUE tenant+key); stores only SHA-256 fingerprint (never secret material); same-key+different-fingerprint = explicit 409 conflict (never silent replay); replay returns original result without repeating reservation/job creation; compensation releases reservation on job-creation failure.
- Quota reservation expiry/reaper: active reservations have durable expires_at; renewReservation fenced by version (stale writer cannot extend); reapExpired idempotent + fenced (run twice = no double-release; capacity released exactly once); expired orphan after reservation-before-job-creation is recovered. Distinction documented: admission reservation (economic capacity) vs execution ownership lease (write authority) vs worker heartbeat (liveness) — NOT interchangeable.
- Durable billing settlement: UsageEvent→classify→resolve immutable PricingVersion→exactly-one LedgerEntry; states pending→priced→settled / non_billable / unresolved; unresolved is durable+retryable (never silently dropped); pricing changes cannot rewrite historical charges; ledger history immutable (corrections = new entries).
- Runtime economic enforcement: ExecutionPolicy gains optional soft maxTokens/maxDurationMs; checked at step boundaries (bounded overshoot of one turn); budget exhaustion emits budget_exhausted event + cancels through a durable state preserving the checkpoint boundary — checkpoint correctness is NEVER sacrificed for billing/quota.
- API-key lifecycle: expiry/rotation (rotated_from + overlap_expires_at)/revocation/last-used (async best-effort, never an auth bottleneck); authorization uses authoritative key state not cached last-used; never expose stored secret.
- Snapshot GC: lifecycle active→eligible_for_gc→deleting→deleted; conservative (never deletes active recovery path); retry-safe (failed deletion stays deleting); deleted only on provider confirmation; tenant-isolated.
- Operational health: tenant-scoped GET /reconcile/health + GET /operations/health (unresolved usage/pricing, orphaned reservations, settlement/snapshot-GC backlog, last watermark); ordinary tenant credential can never read another tenant's operational data.
- Migration ledger fix (cross-cutting): schema_migrations now deduplicates by migration NAME (globally unique), not version number — different packages intentionally use overlapping version numbers (identity v2 vs store-sql v2), so version-only dedup silently skipped migrations and left tables uncreated. PK changed version→name. This is what lets all stores share one database (required for single-DB reconciliation + PGlite conformance).
- PG validation: Tier A PGlite (real PostgreSQL engine, ALWAYS runs) proves SQL-level invariants — 9 passed; Tier B multi-connection server gated on PG_TEST_* (2 skipped honestly when unavailable). PG skips are reported as skips, never passes.
- Invariants that must not regress: reconciliation never invokes AgentRunner execution (reads listEvents only, projects forward); no stale writer mutates ownership/reservation state (fenced CAS everywhere); no cross-tenant economic/audit read; no plaintext API keys/secrets in SQL/logs/events/audit/errors; pricing+ledger history immutable; policy/job identity immutable after admission; exactly-once at durable usage/ledger identity boundaries (execution stays at-least-once); reaper idempotent + fenced; snapshot GC never deletes active recovery artifact; failed provider deletion stays retryable (never falsely deleted).
