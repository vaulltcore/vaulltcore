{
  "name": "vaulltcore",
  "private": true,
  "version": "0.0.1",
  "description": "Vaulltcore — B2B AI Engineering Automation platform. Phase 1A execution foundation.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --build packages/vaulltcore-runner packages/vaulltcore-runner-opencode"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
